__pycache__/
*.pyc
*.egg-info/
build/
dist/

# Recordings are evidence you make, not evidence shipped with the instrument.
recordings/

# Tooling caches and the local dev environment.
.venv/
.coverage
htmlcov/
.ruff_cache/
.mypy_cache/

# Scratch tree for `make reproduce` and `make tamper-drill`, which run against
# a copy so the checkout is never dirtied by a gate.
.make-work/
